# HTML Editor - Real Typing Animation + Controls

Fixes/Additions:
- Keyboard animation now reveals text letter-by-letter using one step per character.
- Adds blinking cursor for typing animation.
- Adds animation speed / cycle duration control.
- Adds animation repeat / duration control.
- Pulse can now be slowed down or sped up and set to once, multiple cycles, or infinite.
- Glow/neon/shadow/outline/background/echo effects can now use a selected effect color.
- Export includes the updated animation CSS.
- Keeps Canva-like text toolbar, real desktop/mobile gradient separation, move handle, carousel, ZIP import and autosave fixes.
