# HTML Editor - Undo Scroll Preserve Fix

Fix:
- Undo/Ctrl+Z no longer jumps to the top of the page.
- Redo/Ctrl+Y no longer jumps to the top of the page.
- The editor preserves current scroll position and selection across undo/redo.
- Keeps group/multi-select move, carousel wheel passthrough, stable mobile layout, basic elements, transparent PNG, ZIP import, autosave, and carousel fixes.
