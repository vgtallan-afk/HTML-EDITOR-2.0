# HTML Editor - Stable Mobile Layout + Basic Elements

Fixes:
- Mobile Edit no longer freezes when switching from Desktop.
- Removed heavy full-page mobile snapshot.
- Mobile-specific layout is saved only when using controls in Mobile Edit: size, spacing, position, alignment, image size, font size.
- Content remains shared across desktop and mobile.
- Desktop layout changes should not overwrite mobile-specific overrides.
- Add panel includes draggable Text, Image placeholder, and CTA Button.
- Keeps carousel swipe lock, transparent PNG, ZIP import, autosave, desktop/mobile, and full-bleed fixes.
