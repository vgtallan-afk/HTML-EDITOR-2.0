# HTML Editor - Carousel Vertical Wheel Passthrough

Fix:
- In mobile preview on desktop, the mouse wheel now scrolls vertically even when the pointer is over the carousel.
- The carousel only captures horizontal gestures.
- Vertical wheel movement is manually passed to the page scroll, so iframe/mobile-preview behavior no longer blocks it.
- Keeps previous stable mobile layout, basic elements, transparent PNG, ZIP import, autosave, and carousel fixes.
