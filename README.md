# HTML Editor - Carousel Scroll/Wheel Fix

Fix:
- Mouse wheel over carousel now scrolls the page vertically normally.
- Vertical touch gestures over carousel scroll the page normally.
- Carousel only blocks vertical scrolling during a real horizontal swipe/drag.
- Horizontal mouse/trackpad gestures can still move the carousel.
- Keeps previous stable mobile layout, basic elements, transparent PNG, ZIP import, autosave, and carousel fixes.
